// Ordem de prioridade no JavaScript:
// Parênteses ( )

// Multiplicação *, Divisão /

// Adição +, Subtração -

// Comparações (>, <, >=, <=, ==, !=)

// Operadores lógicos (!, &&, ||)

// Sempre que ficar na dúvida, use parênteses para deixar bem claro qual é a ordem que você deseja que o JavaScript execute as operações.